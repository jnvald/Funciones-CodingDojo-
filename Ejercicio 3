//ebes imprimir la suma de todos los valores en el arreglo dado:

function printSumArray(x) {
    var sum = 0;
    for (var i = 0; i < x.length; i++) {
        // escribe tu código aquí
    }
    return sum;
}
console.log( printSumArray([1,2,3]) ); // debe imprimir 6
